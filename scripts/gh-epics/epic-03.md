**Epic 3 — Runtime scanner**

## Stories

- Ollama binary + version + localhost reachability (timeouts)
- MLX / `mlx` pip detection
- Configured local inference endpoints (future)
- Runtime status model (`installed` / `reachable` / confidence)

## Acceptance

- States are consistent; timeouts never crash the full scan.
