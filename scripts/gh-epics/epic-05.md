**Epic 5 — Model scanner**

## Stories

- Ollama model enumeration via local API
- MLX-managed paths (when defined)
- Normalise IDs; attach `runtime_id`; locality; quantisation when exposed

## Acceptance

- Merge is stable; ambiguous rows marked inferred/unknown — no fabricated metadata.
