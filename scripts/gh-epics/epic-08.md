**Epic 8 — Submission service**

## Stories

- API payload contract (document with server)
- Client retries / errors
- Local receipt log under `~/.impact/`

## Acceptance

- Failures never delete local artifacts; success returns a receipt id.
