**Epic 9 — Rules engine (readiness hints)**

## Stories

- Conservative heuristics (no benchmark claims)
- Map machine + runtimes + models → coarse guidance
- Surface in HTML + JSON `readiness` (optional)

## Acceptance

- Explainable copy; `unknown` when data insufficient.
