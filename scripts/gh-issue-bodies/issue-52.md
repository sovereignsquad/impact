## Objective

**Historical tools / runtimes dataset MVP**: counts by **tool/runtime family** (e.g. Ollama, MLX, allowlisted tools as they appear in profiles).

## Constraints

- Real counts only from ingest; **clearly labelled** placeholders otherwise.
- No leaderboard hype; **aggregates** only.

## Scope

- Categories align with profile schema / scanner allowlist ([product.md](../../docs/product.md)).
- Optional mention of future tools (OpenClaw, Paperclip, VSCodium integrations) as **categories** only when data exists or as “tracked when reported”.

## Acceptance

- [ ] UI section exists under historical/tools path.
- [ ] Honest empty state or real data; no fake bars.

## Board

**Backlog (SOONER)** — P0 data slice.
