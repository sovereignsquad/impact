## Objective

**Standalone binary packaging** — spike: `pkg`, `bun build --compile`, or equivalent for `impact` without Node install.

## Scope

Feasibility, size, signing gaps, CI story; recommendation doc.

## Based on

[Issue #1](https://github.com/moldovancsaba/impact/issues/1) — Backlog; informs distribution decision with **#34**.
